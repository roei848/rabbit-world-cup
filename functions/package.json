{
  "name": "black-rabbit-wc-functions",
  "private": true,
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint --ext .ts src/",
    "test": "vitest run"
  },
  "engines": { "node": "22" },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.0.0"
  }
}
