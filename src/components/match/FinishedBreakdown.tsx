import type { MatchDoc } from '../../firebase/firestore';

interface FinishedBreakdownProps {
  match: MatchDoc;
}

// Phase 3 stub — filled out in Phase 4 when pick data is available
export function FinishedBreakdown(_props: FinishedBreakdownProps) {
  return null;
}
